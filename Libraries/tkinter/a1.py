import tkinter as tk

root = tk.Tk() #Create a window 
root.title("OB Timer") #Title of the window
root.geometry("350x250") #Size of the window

root.mainloop() #The engine let the window work all time 
